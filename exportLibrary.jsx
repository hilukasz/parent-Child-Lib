function textIsIn(child, parentItem){    var left = Math.round(child.anchor[0]),        bottom = Math.round(child.anchor[1]);    var parentVB = parentItem.visibleBounds,        parentLeft = parentVB[0],        parentTop = Math.abs(parentVB[1]),        parentRight = parentVB[2],        parentBottom = Math.abs(parentVB[3]);            //$.write(child.contents+" :  [ left: "+left+", bottom: "+Math.abs(bottom)+"]  \n\n" );    //$.write(parentItem.name+" :   [ left: "+parentLeft+", top: "+parentTop+", right: "+parentRight+", bottom: "+parentBottom+"]  \n\n" );    //return true if child is in parent    if(left >= parentLeft &&  left <= parentRight && bottom <= parentBottom  &&  bottom >= parentTop){        print("returns true");     }        return left >= parentLeft &&  left <= parentRight &&           bottom <= parentBottom  &&  bottom >= parentTop;}